@model IEnumerable<AdministrareEvenimenteWeb.Models.Model>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Model1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Culoare)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Model1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Culoare)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdModel">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdModel">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdModel">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
