@model IEnumerable<AdministrareEvenimenteWeb.Models.Contract>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdClientNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdServiciiNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdClientNavigation.IdClient)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdServiciiNavigation.IdServicii)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdContract">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdContract">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdContract">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
