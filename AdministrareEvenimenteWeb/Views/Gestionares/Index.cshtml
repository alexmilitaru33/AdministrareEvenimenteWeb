@model IEnumerable<AdministrareEvenimenteWeb.Models.Gestionare>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEvenimentNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdLocatieEvenimentNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdEvenimentNavigation.IdEveniment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdLocatieEvenimentNavigation.IdLocatieEveniment)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdGestionare">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdGestionare">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdGestionare">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
