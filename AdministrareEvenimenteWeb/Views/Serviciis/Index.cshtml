@model IEnumerable<AdministrareEvenimenteWeb.Models.Servicii>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeServiciu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PretServiciu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeServiciu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PretServiciu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdServicii">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdServicii">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdServicii">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
